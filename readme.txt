
1. For following attributes, we supply the caffeNet models in corresponding folders:

body age; body gender; body clothing; head age; head gender; head pose; face appearance; face emotion


2. For following attributes, we extract fc-7 layer features from previously trained models. We give the extracted featureArray in corresponding folders:

imsitu body activity; body immediacy


3. For body/face location and relative scale, we supply the statistic data in correponding folder. Only version of annotator_consistency3 (the one used in our paper) is given.
